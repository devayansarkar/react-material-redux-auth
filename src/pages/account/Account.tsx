import React, { Component } from 'react';

class Account extends Component {
    render() {
        return <h1>Hello, Account page. You can only see this if you are authenticated.</h1>;
    }
}

export default Account;